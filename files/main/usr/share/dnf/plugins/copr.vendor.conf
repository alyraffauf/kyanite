# Force dnf5's copr plugin to use the "fedora" chroot regardless of what
# /etc/os-release reports as ID. Without this, `dnf5 copr enable foo/bar`
# fails because COPR doesn't have a "kyanite-44" chroot — it only has
# fedora-44.
[main]
distribution = fedora
